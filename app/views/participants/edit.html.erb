<h1>Edit Participant</h1>

<%= render 'form', participant: @participant %>

<hr>
<%= link_to 'Participant', participants_path, class: 'btn btn-primary' %>
<%= link_to 'Show', participant_path(@participant), class: 'btn btn-primary' %>
